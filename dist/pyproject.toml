[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tessera"
version = "1.0.0"
description = "Tessera v1 — file-system-native ticket runtime framework"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
dependencies = [
    "pyyaml>=6.0",
    "inotify-simple>=1.3.5",
    "typer>=0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[project.scripts]
ticket = "tessera_runtime.cli:main"
tessera = "tessera_runtime.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/tessera_sdk", "src/tessera_runtime"]

[tool.pytest.ini_options]
testpaths = ["tests"]
