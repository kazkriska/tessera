"""Tessera CLI entry point.

Responsibility (Master Part XI, CONTRACTS §7): a `typer` application that talks
to the running daemon over `runtime.sock` (ticket create/list/run/status, repo
boot, etc.).

TODO(Phase G): build the typer app and socket client.
"""


def main() -> None:
    """Console-script entry point. Not implemented in Phase A."""
    raise NotImplementedError("tessera CLI is not implemented yet (Phase G)")
